const searches = require('./searches');
const albums = require('./albums');
const releases = require('./releases');
const results = require('./results');
const credits = require('./credits');

module.exports = ({
  searches, albums, releases, results, credits,
});
